const {app: vanilla} = require("../../apps/system-api")
const {app: matter} = require("../../apps/system-api")
const {app: substance} = require("../../apps/system-api")
const {app: surface} = require("../../apps/system-api")
const {app: realms} = require("../../apps/system-api")

module.exports = {
    vanilla,
    matter,
    substance,
    surface,
    realms 
}